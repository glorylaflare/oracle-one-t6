// 1
let contadorCresc = 1;
while(contadorCresc <= 10) {
    console.log(contadorCresc);
    contadorCresc++;
};

// 2
let contadorDesc = 10;
while(contadorDesc >= 0) {
    console.log(contadorDesc);
    contadorDesc--;
};

// 3
let contadorRegressivo = prompt("Escolha um número entre 1 e 100");
while(contadorRegressivo >= 0) {
    console.log(contadorRegressivo);
    contadorRegressivo--;10

};

// 4
let contadorProgressivo = prompt("Escolha um número entre 1 e 100"), contagem = 0;
while(contagem <= contadorProgressivo) {
    console.log(contagem);
    contagem++;
};